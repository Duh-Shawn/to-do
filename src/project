function Project() {
    const todoList = [];

    const addToDo = (toDo) => {
        todoList.push(toDo);
    }

    const getList = () => todoList;

    return { addToDo, getList };

}

export default Project;